spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/AutoStackDB
    username: postgres
    password: Vansh12lute
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        format_sql: true
