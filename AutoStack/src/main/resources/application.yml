spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/AutoStackDB
    username: postgres
    password: Vansh12lute
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true



server:
  port: 8081
  servlet:
    context-path: /api/v1
